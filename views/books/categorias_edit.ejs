<!DOCTYPE html>
<html lang="es">
<head>
  <meta charset="utf-8">
  <meta name="viewport" content="width=device-width, initial-scale=1">
  <title>Editar Categoría</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
</head>
<body class="d-flex justify-content-center align-items-center min-vh-100">
  <% if (messages.error || messages.success) { %>
  <div class="position-fixed top-0 start-50 translate-middle-x mt-3" style="z-index: 1000;">
    <div class="alert <%= messages.error ? 'alert-danger' : 'alert-success' %> alert-dismissible fade show" role="alert">
      <i class="fas <%= messages.error ? 'fa-exclamation-circle' : 'fa-check-circle' %> me-2"></i>
      <%= messages.error || messages.success %>
      <button type="button" class="btn-close" data-bs-dismiss="alert" aria-label="Cerrar"></button>
    </div>
  </div>
  <% } %>
  <div class="card shadow w-100 mx-3" style="max-width: 600px;">
    <div class="card-header text-center">
      <i class="fas fa-tags me-2"></i>Editar Categoría
    </div>
    <div class="card-body p-4">
      <form action="/categorias/update/<%= id %>" method="post" id="categoriaEditForm" novalidate>
        <div class="mb-4">
          <label class="form-label">Nombre de la Categoría:</label>
          <div class="input-group">
            <span class="input-group-text"><i class="fas fa-heading"></i></span>
            <input type="text" class="form-control" name="name" value="<%= name %>" placeholder="Nombre de la categoría" required>
          </div>
        </div>
        <div class="mb-4">
          <label class="form-label">Descripción:</label>
          <div class="input-group">
            <span class="input-group-text"><i class="fas fa-align-left"></i></span>
            <input type="text" class="form-control" name="description" value="<%= description %>" placeholder="Descripción de la categoría">
          </div>
        </div>
        <button type="submit" class="btn btn-primary w-100">
          <span class="submit-text">Guardar Cambios</span>
        </button>
      </form>
    </div>
  </div>
  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
