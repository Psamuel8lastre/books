<!DOCTYPE html>
<html lang="es">
<head>
  <meta charset="utf-8">
  <meta name="viewport" content="width=device-width, initial-scale=1">
  <title>Categorías</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
  <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
  <link rel="stylesheet" href="https://cdn.datatables.net/1.13.6/css/dataTables.bootstrap5.min.css">
  <link rel="stylesheet" href="/stylesheets/style.css">
</head>
<body>
  <div class="autores-container">
    <nav class="sidebar">
      <div class="p-4">
        <h4 class="mb-4 text-center">
          <i class="fas fa-book-open me-2"></i>Biblioteca 
        </h4>
        <hr class="bg-light opacity-25">
        <ul class="nav flex-column">
          <li class="nav-item">
            <a class="nav-link" href="/">
              <i class="fas fa-tachometer-alt me-2"></i>Dashboard
            </a>
          </li>
          <li class="nav-item">
            <a class="nav-link" href="/books">
              <i class="fas fa-book me-2"></i>Libros
            </a>
          </li>
          <li class="nav-item">
            <a class="nav-link" href="/prestamos">
              <i class="fas fa-exchange-alt me-2"></i>Préstamos
            </a>
          </li>
          <li class="nav-item">
            <a class="nav-link" href="/autores">
              <i class="fas fa-pen-fancy me-2"></i>Autores
            </a>
          </li>
          <li class="nav-item">
            <a class="nav-link" href="/editoriales">
              <i class="fas fa-building me-2"></i>Editoriales
            </a>
          </li>
          <li class="nav-item">
            <a class="nav-link active" href="/categorias">
              <i class="fas fa-tags me-2"></i>Categorías
            </a>
          </li>
        </ul>
      </div>
    </nav>
    <main class="autores-main-content w-100">
      <div class="d-flex justify-content-between align-items-center mb-4">
        <h2><i class="fas fa-tags me-2"></i>Lista de Categorías</h2>
        <a href="/categorias/add" class="btn btn-primary">
          <i class="fas fa-plus"></i> Nueva Categoría
        </a>
      </div>
      <% if (messages && (messages.success || messages.error)) { %>
        <div id="floating-alert" class="custom-alert <%= messages.success ? 'custom-alert-success' : 'custom-alert-danger' %>">
          <div class="d-flex align-items-center">
            <div class="me-3">
              <% if (messages.success) { %>
                <i class="fas fa-check-circle fa-2x"></i>
              <% } else { %>
                <i class="fas fa-exclamation-circle fa-2x"></i>
              <% } %>
            </div>
            <div class="flex-fill">
              <%= messages.success || messages.error %>
            </div>
          </div>
        </div>
        <style>
        #floating-alert.custom-alert {
          position: fixed;
          top: 32px;
          right: 32px;
          z-index: 2000;
          margin: 0;
          min-width: 320px;
          max-width: 420px;
          opacity: 0.98;
          animation: fadeInDown 0.7s;
        }
        @media (max-width: 600px) {
          #floating-alert.custom-alert {
            right: 10px;
            top: 10px;
            min-width: 200px;
            max-width: 95vw;
          }
        }
        @keyframes fadeInDown {
          from { opacity: 0; transform: translateY(-30px); }
          to { opacity: 0.98; transform: translateY(0); }
        }
        .custom-alert {
          border-radius: 12px;
          padding: 1rem 1.5rem;
          font-size: 1.08rem;
          font-weight: 500;
          box-shadow: 0 4px 18px rgba(0,0,0,0.07);
          display: flex;
          align-items: center;
          gap: 1rem;
          transition: opacity 0.5s, top 0.5s;
        }
        .custom-alert-danger {
          background: #fee2e2;
          color: #b91c1c;
          border-left: 6px solid #ef4444;
        }
        .custom-alert-success {
          background: #d1fae5;
          color: #065f46;
          border-left: 6px solid #10b981;
        }
        </style>
        <script>
        document.addEventListener('DOMContentLoaded', function() {
          const alert = document.getElementById('floating-alert');
          if(alert) {
            setTimeout(() => {
              alert.style.opacity = '0';
              alert.style.top = '0px';
              setTimeout(() => alert.remove(), 600);
            }, 3500);
          }
        });
        </script>
      <% } %>
      <% if (data.length) { %>
        <div class="categorias-cards-list row g-4 animate-fade-in">
          <% data.forEach(function(categoria, i) { 
            var gradClass = 'categoria-gradient-' + ((i%6)+1); 
          %>
            <div class="col-12 col-sm-6 col-md-4 col-lg-3">
              <div class="categoria-card card h-100 text-center p-3 border-0 shadow-sm <%= gradClass %>">
                <div class="categoria-card-icon mx-auto mb-3">
                  <span class="categoria-avatar-circle">
                    <i class="fas fa-tag"></i>
                  </span>
                </div>
                <h5 class="fw-bold mb-1"><%= categoria.name %></h5>
                <div class="text-muted mb-2" style="font-size:0.97rem"><%= categoria.description || '-' %></div>
                <div class="d-flex justify-content-center gap-2 mt-auto">
                  <a href="/categorias/edit/<%= categoria.id %>" class="btn btn-sm btn-outline-success px-3">
                    <i class="fas fa-edit"></i>
                  </a>
                  <a href="/categorias/delete/<%= categoria.id %>" class="btn btn-sm btn-outline-danger px-3" onclick="return confirm('¿Estás seguro?')">
                    <i class="fas fa-trash-alt"></i>
                  </a>
                </div>
              </div>
            </div>
          <% }); %>
        </div>
      <% } else { %>
        <div class="text-center py-5">
          <img src="https://cdn-icons-png.flaticon.com/512/2748/2748558.png" width="100" class="mb-3 opacity-50">
          <h5 class="text-muted">No hay categorías registradas</h5>
          <a href="/categorias/add" class="btn btn-primary mt-3">
            <i class="fas fa-plus-circle"></i> Agregar Categoría
          </a>
        </div>
      <% } %>
    </main>
  </div>
  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
