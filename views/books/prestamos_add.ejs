<!DOCTYPE html>
<html lang="es">
<head>
  <meta charset="utf-8">
  <meta name="viewport" content="width=device-width, initial-scale=1">
  <title>Agregar Préstamo</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
</head>
<body class="d-flex justify-content-center align-items-center min-vh-100">
  <% if (messages.error || messages.success) { %>
  <div class="position-fixed top-0 start-50 translate-middle-x mt-3" style="z-index: 1000;">
    <div class="alert <%= messages.error ? 'alert-danger' : 'alert-success' %> alert-dismissible fade show" role="alert">
      <i class="fas <%= messages.error ? 'fa-exclamation-circle' : 'fa-check-circle' %> me-2"></i>
      <%= messages.error || messages.success %>
      <button type="button" class="btn-close" data-bs-dismiss="alert" aria-label="Cerrar"></button>
    </div>
  </div>
  <% } %>
  <div class="card shadow w-100 mx-3" style="max-width: 600px;">
    <div class="card-header text-center">
      <i class="fas fa-exchange-alt me-2"></i>Agregar Nuevo Préstamo
    </div>
    <div class="card-body p-4">
      <form action="/prestamos/add" method="post" id="prestamoForm" novalidate>
        <div class="mb-4">
          <label class="form-label">Libro:</label>
          <div class="input-group">
            <span class="input-group-text"><i class="fas fa-book"></i></span>
            <input type="text" class="form-control" name="libro" value="<%= libro %>" placeholder="Nombre del libro" required>
          </div>
        </div>
        <div class="mb-4">
          <label class="form-label">Usuario:</label>
          <div class="input-group">
            <span class="input-group-text"><i class="fas fa-user"></i></span>
            <input type="text" class="form-control" name="usuario" value="<%= usuario %>" placeholder="Nombre del usuario" required>
          </div>
        </div>
        <div class="mb-4">
          <label class="form-label">Fecha de Préstamo:</label>
          <div class="input-group">
            <span class="input-group-text"><i class="fas fa-calendar"></i></span>
            <input type="date" class="form-control" name="fecha_prestamo" value="<%= fecha_prestamo %>" required>
          </div>
        </div>
        <div class="mb-4">
          <label class="form-label">Fecha de Devolución:</label>
          <div class="input-group">
            <span class="input-group-text"><i class="fas fa-calendar-check"></i></span>
            <input type="date" class="form-control" name="fecha_devolucion" value="<%= fecha_devolucion %>" required>
          </div>
        </div>
        <div class="mb-4">
          <label class="form-label">Estado:</label>
          <div class="input-group">
            <span class="input-group-text"><i class="fas fa-info-circle"></i></span>
            <input type="text" class="form-control" name="estado" value="<%= estado %>" placeholder="Ej: En préstamo, Devuelto, Vencido">
          </div>
        </div>
        <button type="submit" class="btn btn-primary w-100">
          <span class="submit-text">Agregar Préstamo</span>
        </button>
      </form>
    </div>
  </div>
  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
