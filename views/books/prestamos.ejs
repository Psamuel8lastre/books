<!DOCTYPE html>
<html lang="es">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Biblioteca | Préstamos</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
  <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
  <link rel="stylesheet" href="/stylesheets/style.css">
  <link rel="stylesheet" href="/stylesheets/index.css">
  <link rel="icon" href="/images/bibiotecalogo-removebg-preview.png" type="image/x-icon">
</head>
<body>
  <div class="wrapper d-flex">
    <!-- Sidebar -->
    <div class="sidebar col-md-3 col-lg-2 p-0">
      <div class="p-4">
        <h4 class="mb-4 text-center">
          <i class="fas fa-book-open me-2"></i>Biblioteca 
        </h4>
        <hr class="bg-light opacity-25">
        <ul class="nav flex-column">
          <li class="nav-item">
            <a class="nav-link" href="/">
              <i class="fas fa-tachometer-alt me-2"></i>Dashboard
            </a>
          </li>
          <li class="nav-item">
            <a class="nav-link" href="/books">
              <i class="fas fa-book me-2"></i>Libros
            </a>
          </li>
          <li class="nav-item">
            <a class="nav-link active" href="/prestamos">
              <i class="fas fa-exchange-alt me-2"></i>Préstamos
            </a>
          </li>
          <li class="nav-item">
            <a class="nav-link" href="/autores">
              <i class="fas fa-pen-fancy me-2"></i>Autores
            </a>
          </li>
          <li class="nav-item">
            <a class="nav-link" href="/editoriales">
              <i class="fas fa-building me-2"></i>Editoriales
            </a>
          </li>
          <li class="nav-item">
            <a class="nav-link" href="/categorias">
              <i class="fas fa-tags me-2"></i>Categorías
            </a>
          </li>
        </ul>
      </div>
    </div>

    <!-- Main Content -->
    <div class="main-content col-md-9 col-lg-10 p-0 bg-light">
      <!-- Top Bar -->
      <nav class="navbar navbar-expand navbar-light bg-darck shadow-sm">
        <div class="container-fluid">
          <div class="search-box ms-3 w-50">
            <i class="fas fa-search"></i>
            <input class="form-control" type="search" placeholder="Buscar préstamos..." id="globalSearch">
          </div>
        </div>
      </nav>

      <div class="container-fluid p-4">
        <div class="cita-literaria mb-4">
          <i class="fas fa-quote-left me-2"></i>
          “Un préstamo de un libro es un puente entre dos sueños.”
          <span class="d-block text-end mt-2" style="font-size:0.95rem;color:#b45309;">— Adaptación</span>
        </div>
        <!-- Alerta de bienvenida o error -->
        <% if (messages && (messages.success || messages.error)) { %>
          <div id="floating-alert" class="custom-alert <%= messages.success ? 'custom-alert-success' : 'custom-alert-danger' %>">
            <div class="d-flex align-items-center">
              <div class="me-3">
                <% if (messages.success) { %>
                  <i class="fas fa-check-circle fa-2x"></i>
                <% } else { %>
                  <i class="fas fa-exclamation-circle fa-2x"></i>
                <% } %>
              </div>
              <div class="flex-fill">
                <%= messages.success || messages.error %>
              </div>
            </div>
          </div>
        <% } %>
        <script>
        document.addEventListener('DOMContentLoaded', function() {
          const alert = document.getElementById('floating-alert');
          if(alert) {
            setTimeout(() => {
              alert.style.opacity = '0';
              alert.style.top = '0px';
              setTimeout(() => alert.remove(), 600);
            }, 3500);
          }
        });
        </script>

        <!-- Encabezado con estadísticas -->
        <div class="card shadow mb-5">
          <div class="card-header bg-white d-flex justify-content-between align-items-center py-3">
            <h5 class="mb-0 text-primary">
              <i class="fas fa-exchange-alt me-2"></i>Préstamos Activos
            </h5>
            <a href="/prestamos/add" class="btn btn-primary">
              <i class="fas fa-plus-circle"></i> Nuevo Préstamo
            </a>
          </div>
          <div class="card-body">
            <div class="row mb-4">
              <div class="col-md-4">
                <div class="stat-card total">
                  <div class="stat-value"><%= typeof totalPrestamos !== 'undefined' ? totalPrestamos : 0 %></div>
                  <div class="stat-label">Total Préstamos</div>
                  <i class="stat-icon fas fa-book"></i>
                </div>
              </div>
              <div class="col-md-4">
                <div class="stat-card active">
                  <div class="stat-value"><%= typeof prestamosActivos !== 'undefined' ? prestamosActivos : 0 %></div>
                  <div class="stat-label">Activos</div>
                  <i class="stat-icon fas fa-check-circle"></i>
                </div>
              </div>
              <div class="col-md-4">
                <div class="stat-card overdue">
                  <div class="stat-value"><%= typeof prestamosVencidos !== 'undefined' ? prestamosVencidos : 0 %></div>
                  <div class="stat-label">Vencidos</div>
                  <i class="stat-icon fas fa-exclamation-circle"></i>
                </div>
              </div>
            </div>

            <!-- Cards de préstamos -->
            <% if (data && data.length) { %>
              <div class="loans-grid">
                <% data.forEach(function(prestamo, i) { %>
                  <div class="loan-card <%= prestamo.estado ? prestamo.estado.toLowerCase().replace(' ', '-') : '' %>">
                    <div class="loan-header">
                      <span class="loan-id">#<%= i+1 %></span>
                      <span class="loan-status"><%= prestamo.estado || '-' %></span>
                    </div>
                    <div class="loan-book">
                      <i class="fas fa-book"></i>
                      <span><%= prestamo.libro %></span>
                    </div>
                    <div class="loan-user">
                      <i class="fas fa-user"></i>
                      <span><%= prestamo.usuario %></span>
                    </div>
                    <div class="loan-dates">
                      <div class="date-item">
                        <span class="date-label">Préstamo:</span>
                        <span class="date-value"><%= prestamo.fecha_prestamo ? (prestamo.fecha_prestamo.toLocaleDateString ? prestamo.fecha_prestamo.toLocaleDateString() : prestamo.fecha_prestamo) : '-' %></span>
                      </div>
                      <div class="date-item">
                        <span class="date-label">Devolución:</span>
                        <span class="date-value"><%= prestamo.fecha_devolucion ? (prestamo.fecha_devolucion.toLocaleDateString ? prestamo.fecha_devolucion.toLocaleDateString() : prestamo.fecha_devolucion) : '-' %></span>
                      </div>
                    </div>
                    <div class="loan-actions">
                      <a href="/prestamos/edit/<%= prestamo.id %>" class="btn btn-edit">
                        <i class="fas fa-edit"></i> Editar
                      </a>
                      <a href="/prestamos/delete/<%= prestamo.id %>" class="btn btn-delete" onclick="return confirm('¿Estás seguro?')">
                        <i class="fas fa-trash-alt"></i> Eliminar
                      </a>
                    </div>
                  </div>
                <% }); %>
              </div>
            <% } else { %>
              <div class="text-center py-5">
                <img src="https://cdn-icons-png.flaticon.com/512/2748/2748558.png" width="100" class="mb-3 opacity-50">
                <h5 class="text-muted">No hay préstamos registrados</h5>
                <p class="text-muted">Comienza agregando tu primer préstamo</p>
                <a href="/prestamos/add" class="btn btn-primary">
                  <i class="fas fa-plus-circle"></i> Agregar Préstamo
                </a>
              </div>
            <% } %>
          </div>
        </div>
      </div>
      <footer class="app-footer">
        <div class="footer-bottom">
          <div class="copyright">
            &copy; 2023 Biblioteca Digital. Todos los derechos reservados.
          </div>
          <div class="legal-links">
            <a href="#" class="legal-link">Términos de servicio</a>
            <a href="#" class="legal-link">Política de privacidad</a>
            <a href="#" class="legal-link">Aviso legal</a>
          </div>
        </div>
      </footer>
    </div>
  </div>
  <!-- Scripts -->
  <script src="https://code.jquery.com/jquery-3.6.0.min.js"></script>
  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
  <script>
    $(document).ready(function() {
      // Filtro de búsqueda global para préstamos (busca en texto de las tarjetas)
      $('#globalSearch').on('keyup', function() {
        var value = this.value.toLowerCase();
        $('.loan-card').each(function() {
          var cardText = $(this).text().toLowerCase();
          $(this).toggle(cardText.indexOf(value) > -1);
        });
      });
    });
  </script>
  <style>
    /* ...estilos de tarjetas y alertas, igual que antes... */
    .stat-card {
      background: #fff;
      border-radius: 1rem;
      box-shadow: 0 2px 12px 0 rgba(99,102,241,0.10);
      padding: 1.1rem 1.1rem 0.8rem 1.1rem;
      min-height: 110px;
      margin-bottom: 1rem;
      display: flex;
      flex-direction: column;
      align-items: center;
      justify-content: center;
      font-size: 1.04rem;
    }
    .stat-value {
      font-size: 2.1rem;
      font-weight: bold;
      color: #6366f1;
    }
    .stat-label {
      font-size: 1.1rem;
      color: #64748b;
      margin-top: 0.2rem;
    }
    .stat-icon {
      font-size: 1.7rem;
      margin-top: 0.3rem;
      color: #b45309;
    }
    .loans-grid {
      display: grid;
      grid-template-columns: repeat(auto-fit, minmax(260px, 1fr));
      gap: 1.1rem;
    }
    .loan-card {
      background: #fff;
      border-radius: 1rem;
      box-shadow: 0 2px 12px 0 rgba(99,102,241,0.10);
      padding: 1.1rem 1.1rem 0.8rem 1.1rem;
      min-height: 150px;
      max-width: 260px;
      margin: 0 auto;
      display: flex;
      flex-direction: column;
      justify-content: space-between;
      transition: box-shadow 0.2s, transform 0.2s;
      font-size: 1.04rem;
    }
    .loan-card:hover {
      box-shadow: 0 6px 24px #10b98133;
      transform: translateY(-3px) scale(1.03);
    }
    .loan-header {
      display: flex;
      justify-content: space-between;
      align-items: center;
      font-size: 1.05rem;
      font-weight: 600;
      color: #6366f1;
      margin-bottom: 0.4rem;
    }
    .loan-status {
      font-size: 1rem;
      padding: 0.22rem 0.9rem;
      border-radius: 1rem;
      background: #f0fdf4;
      color: #10b981;
      font-weight: 500;
    }
    .loan-card.overdue .loan-status, .loan-card.vencido .loan-status {
      background: #fee2e2;
      color: #b91c1c;
    }
    .loan-card.active .loan-status, .loan-card.activo .loan-status {
      background: #d1fae5;
      color: #10b981;
    }
    .loan-book, .loan-user {
      display: flex;
      align-items: center;
      gap: 0.6rem;
      font-size: 1.05rem;
      color: #6366f1;
      margin-bottom: 0.2rem;
    }
    .loan-book i, .loan-user i {
      color: #10b981;
    }
    .loan-dates {
      font-size: 0.98rem;
      color: #64748b;
      margin-bottom: 0.3rem;
    }
    .date-item {
      display: flex;
      justify-content: space-between;
    }
    .loan-actions {
      display: flex;
      gap: 0.6rem;
      margin-top: 0.3rem;
    }
    .btn-edit, .btn-delete {
      font-size: 0.98rem;
      padding: 0.22rem 0.9rem;
      border-radius: 0.5rem;
      border: none;
      transition: background 0.2s, color 0.2s;
    }
    .btn-edit {
      background: #f0fdf4;
      color: #10b981;
    }
    .btn-edit:hover {
      background: #10b981;
      color: #fff;
    }
    .btn-delete {
      background: #fee2e2;
      color: #b91c1c;
    }
    .btn-delete:hover {
      background: #b91c1c;
      color: #fff;
    }
    @media (max-width: 600px) {
      .loans-grid {
        grid-template-columns: 1fr;
        gap: 0.7rem;
      }
      .loan-card {
        max-width: 98vw;
        padding: 0.7rem 0.4rem 0.4rem 0.4rem;
        min-height: 120px;
        font-size: 0.98rem;
      }
    }
  </style>
</body>
</html>