<!DOCTYPE html>
<html lang="es">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Biblioteca | Editoriales</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
  <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
  <link rel="stylesheet" href="/stylesheets/style.css">
  <link rel="stylesheet" href="/stylesheets/index.css">
  <link rel="icon" href="/images/bibiotecalogo-removebg-preview.png" type="image/x-icon">
</head>
<body>
  <div class="wrapper d-flex">
    <!-- Sidebar -->
    <div class="sidebar col-md-3 col-lg-2 p-0">
      <div class="p-4">
        <h4 class="mb-4 text-center">
          <i class="fas fa-book-open me-2"></i>Biblioteca 
        </h4>
        <hr class="bg-light opacity-25">
        <ul class="nav flex-column">
          <li class="nav-item">
            <a class="nav-link" href="/">
              <i class="fas fa-tachometer-alt me-2"></i>Dashboard
            </a>
          </li>
          <li class="nav-item">
            <a class="nav-link" href="/books">
              <i class="fas fa-book me-2"></i>Libros
            </a>
          </li>
          <li class="nav-item">
            <a class="nav-link" href="/prestamos">
              <i class="fas fa-exchange-alt me-2"></i>Préstamos
            </a>
          </li>
          <li class="nav-item">
            <a class="nav-link" href="/autores">
              <i class="fas fa-pen-fancy me-2"></i>Autores
            </a>
          </li>
          <li class="nav-item">
            <a class="nav-link active" href="/editoriales">
              <i class="fas fa-building me-2"></i>Editoriales
            </a>
          </li>
          <li class="nav-item">
            <a class="nav-link" href="/categorias">
              <i class="fas fa-tags me-2"></i>Categorías
            </a>
          </li>
        </ul>
      </div>
    </div>

    <!-- Main Content -->
    <div class="main-content col-md-9 col-lg-10 p-0 bg-light">
      <!-- Top Bar -->
      <nav class="navbar navbar-expand navbar-light bg-darck shadow-sm">
        <div class="container-fluid">
          <div class="search-box ms-3 w-50">
            <i class="fas fa-search"></i>
            <input class="form-control" type="search" placeholder="Buscar editoriales..." id="globalSearch">
          </div>
        </div>
      </nav>

      <div class="container-fluid p-4">
        <div class="cita-literaria mb-4">
          <i class="fas fa-quote-left me-2"></i>
          “Una editorial es el puente entre el autor y el lector.”
          <span class="d-block text-end mt-2" style="font-size:0.95rem;color:#b45309;">— Adaptación</span>
        </div>
        <!-- Alerta de bienvenida o error -->
        <% if (messages && (messages.success || messages.error)) { %>
          <div id="floating-alert" class="custom-alert <%= messages.success ? 'custom-alert-success' : 'custom-alert-danger' %>">
            <div class="d-flex align-items-center">
              <div class="me-3">
                <% if (messages.success) { %>
                  <i class="fas fa-check-circle fa-2x"></i>
                <% } else { %>
                  <i class="fas fa-exclamation-circle fa-2x"></i>
                <% } %>
              </div>
              <div class="flex-fill">
                <%= messages.success || messages.error %>
              </div>
            </div>
          </div>
        <% } %>
        <script>
        document.addEventListener('DOMContentLoaded', function() {
          const alert = document.getElementById('floating-alert');
          if(alert) {
            setTimeout(() => {
              alert.style.opacity = '0';
              alert.style.top = '0px';
              setTimeout(() => alert.remove(), 600);
            }, 3500);
          }
        });
        </script>

        <div class="card shadow mb-5">
          <div class="card-header bg-white d-flex justify-content-between align-items-center py-3">
            <h5 class="mb-0 text-primary">
              <i class="fas fa-building me-2"></i>Lista de Editoriales
            </h5>
            <a href="/editoriales/add" class="btn btn-primary">
              <i class="fas fa-plus-circle"></i> Nueva Editorial
            </a>
          </div>
          <div class="card-body">
            <% if (data.length) { %>
              <div class="row g-4 animate-fade-in">
                <% data.forEach(function(editorial, i) { 
                  var gradClass = 'editorial-gradient-' + ((i%6)+1); 
                %>
                  <div class="col-12 col-sm-6 col-md-4 col-lg-3">
                    <div class="editorial-card card h-100 text-center p-3 border-0 shadow-sm <%= gradClass %>">
                      <div class="editorial-card-icon mx-auto mb-3">
                        <span class="editorial-avatar-circle">
                          <i class="fas fa-building"></i>
                        </span>
                      </div>
                      <h5 class="fw-bold mb-1 editorial-card-title"><%= editorial.name %></h5>
                      <div class="text-muted mb-1 editorial-card-country"><i class="fas fa-flag me-1"></i> <%= editorial.country || '-' %></div>
                      <div class="text-secondary mb-1 editorial-card-created" style="font-size:0.93rem"><i class="fas fa-calendar-plus me-1"></i> <%= editorial.created_at ? editorial.created_at.toLocaleString ? editorial.created_at.toLocaleString() : editorial.created_at : '-' %></div>
                      <div class="text-secondary mb-2 editorial-card-updated" style="font-size:0.93rem"><i class="fas fa-calendar-check me-1"></i> <%= editorial.updated_at ? editorial.updated_at.toLocaleString ? editorial.updated_at.toLocaleString() : editorial.updated_at : '-' %></div>
                      <div class="d-flex justify-content-center gap-2 mt-auto editorial-card-actions">
                        <a href="/editoriales/edit/<%= editorial.id %>" class="btn btn-sm btn-outline-success px-3 editorial-btn-edit">
                          <i class="fas fa-edit"></i>
                        </a>
                        <a href="/editoriales/delete/<%= editorial.id %>" class="btn btn-sm btn-outline-danger px-3 editorial-btn-delete" onclick="return confirm('¿Estás seguro?')">
                          <i class="fas fa-trash-alt"></i>
                        </a>
                      </div>
                    </div>
                  </div>
                <% }); %>
              </div>
            <% } else { %>
              <div class="text-center py-5">
                <img src="https://cdn-icons-png.flaticon.com/512/2748/2748558.png" width="100" class="mb-3 opacity-50">
                <h5 class="text-muted">No hay editoriales registradas</h5>
                <p class="text-muted">Comienza agregando tu primera editorial</p>
                <a href="/editoriales/add" class="btn btn-primary">
                  <i class="fas fa-plus-circle"></i> Agregar Editorial
                </a>
              </div>
            <% } %>
          </div>
        </div>
      </div>
      <footer class="app-footer">
        <div class="footer-bottom">
          <div class="copyright">
            &copy; 2023 Biblioteca Digital. Todos los derechos reservados.
          </div>
          <div class="legal-links">
            <a href="#" class="legal-link">Términos de servicio</a>
            <a href="#" class="legal-link">Política de privacidad</a>
            <a href="#" class="legal-link">Aviso legal</a>
          </div>
        </div>
      </footer>
    </div>
  </div>
  <!-- Scripts -->
  <script src="https://code.jquery.com/jquery-3.6.0.min.js"></script>
  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
  <script>
    $(document).ready(function() {
      // Filtro de búsqueda global para editoriales (busca en texto de las tarjetas)
      $('#globalSearch').on('keyup', function() {
        var value = this.value.toLowerCase();
        $('.editorial-card').each(function() {
          var cardText = $(this).text().toLowerCase();
          $(this).toggle(cardText.indexOf(value) > -1);
        });
      });
    });
  </script>
  <style>
    /* ...estilos de tarjetas y alertas, igual que antes... */
    .editorial-card {
      border-radius: 1.2rem;
      background: #e6eaf3;
      box-shadow: 0 2px 12px 0 rgba(99,102,241,0.10);
      transition: transform 0.18s, box-shadow 0.18s;
      min-height: 180px;
      max-width: 260px;
      margin-left: auto;
      margin-right: auto;
      padding: 1.2rem 0.7rem 1rem 0.7rem;
      display: flex;
      flex-direction: column;
      align-items: center;
    }
    .editorial-card:hover {
      transform: translateY(-6px) scale(1.03);
      box-shadow: 0 8px 32px 0 rgba(16,185,129,0.13);
    }
    .editorial-card-icon {
      margin-top: -28px;
    }
    .editorial-avatar-circle {
      width: 48px;
      height: 48px;
      font-size: 1.3rem;
      border: 3px solid #fff;
    }
    .editorial-card h5 {
      font-size: 1.08rem;
    }
    .editorial-card .text-muted,
    .editorial-card .text-secondary {
      font-size: 0.92rem;
    }
    @media (max-width: 600px) {
      .editorial-card { min-height: 120px; max-width: 98vw; border-radius: 0.7rem; padding: 0.7rem 0.3rem 0.7rem 0.3rem; }
      .editorial-avatar-circle { width: 36px; height: 36px; font-size: 1rem; }
    }
  </style>
</body>
</html>
